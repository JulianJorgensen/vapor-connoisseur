extends layout.pug

block content
  tr
    td.alert(class='alert-primary')
  tr
    td.content-wrap
      table(width='100%', cellpadding='0', cellspacing='0')
        tr
          td.content-block Hi there, we've received an inquiery
        tr
          td.content-block
            | Name: #{name}
            | Email: #{email}
            | Phone number: #{phone}
            | Your message: #{message}
